# 🚀 Инструкция по настройке MCP сервера

## Шаг 1: Заполните переменные окружения

1. Откройте файл `.env.example`
2. Скопируйте его содержимое в новый файл `.env`
3. Замените значения на реальные:

```env
SUPABASE_URL=http://147.45.143.147:3000
SUPABASE_ANON_KEY=ваш_настоящий_ключ
```

## Шаг 2: Добавьте MCP сервер в настройки Cline

### Вариант 1: Через интерфейс VS Code

1. Откройте **Command Palette** (Cmd+Shift+P на Mac, Ctrl+Shift+P на Windows/Linux)
2. Введите: `Cline: Open MCP Settings`
3. Добавьте следующую конфигурацию в секцию `mcpServers`:

```json
"supabase-itpi-smeta": {
  "command": "node",
  "args": [
    "/Users/addvibe/projects/itpi_smeta/mcp-servers/supabase-itpi-smeta/index.js"
  ],
  "env": {
    "SUPABASE_URL": "http://147.45.143.147:3000",
    "SUPABASE_ANON_KEY": "ВСТАВЬТЕ_ВАШ_КЛЮЧ_СЮДА"
  }
}
```

### Вариант 2: Ручное редактирование файла настроек

Откройте файл:
```
/Users/addvibe/Library/Application Support/Code/User/globalStorage/saoudrizwan.claude-dev/settings/cline_mcp_settings.json
```

И добавьте конфигурацию выше в секцию `mcpServers`.

## Шаг 3: Полная конфигурация (пример)

Ваш файл `cline_mcp_settings.json` должен выглядеть примерно так:

```json
{
  "mcpServers": {
    "supabase-shema": {
      "command": "node",
      "args": [
        "/путь/к/старому/mcp/index.js"
      ],
      "env": {
        "SUPABASE_URL": "http://5.129.203.41:3000",
        "SUPABASE_ANON_KEY": "ключ_для_старого_сервера"
      }
    },
    "supabase-itpi-smeta": {
      "command": "node",
      "args": [
        "/Users/addvibe/projects/itpi_smeta/mcp-servers/supabase-itpi-smeta/index.js"
      ],
      "env": {
        "SUPABASE_URL": "http://147.45.143.147:3000",
        "SUPABASE_ANON_KEY": "ваш_ключ_для_itpi_smeta"
      }
    },
    "figma-context": {
      "command": "npx",
      "args": [
        "-y",
        "@glips/figma-context-mcp"
      ],
      "env": {
        "FIGMA_PERSONAL_ACCESS_TOKEN": "ваш_figma_токен"
      }
    }
  }
}
```

## Шаг 4: Перезапустите VS Code

После добавления конфигурации **обязательно перезапустите VS Code**, чтобы изменения вступили в силу.

## Шаг 5: Проверка работы

После перезапуска VS Code откройте Cline и попробуйте команду:

```
Покажи список таблиц в базе данных itpi_smeta
```

Если все настроено правильно, вы увидите список таблиц из вашей базы данных на VPS 147.45.143.147.

## ⚠️ Важные замечания

1. **Не коммитьте файл `.env`** в git - он содержит секретные ключи
2. **Замените все плейсхолдеры** (`ваш_ключ`, `ВСТАВЬТЕ_ВАШ_КЛЮЧ_СЮДА`) на реальные значения
3. **Проверьте порт** - по умолчанию PostgREST использует порт 3000, но у вас может быть другой
4. **Оба MCP сервера** (старый и новый) будут работать одновременно

## 🔧 Получение SUPABASE_ANON_KEY

Если вы используете self-hosted Supabase:

1. Подключитесь к вашему VPS: `ssh user@147.45.143.147`
2. Найдите файл конфигурации Supabase (обычно `.env` в директории проекта)
3. Найдите переменную `ANON_KEY` или `SUPABASE_ANON_KEY`
4. Скопируйте значение и используйте его в настройках MCP

Или выполните на VPS:
```bash
docker exec supabase-kong cat /var/lib/kong/kong.yml | grep anon
```

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте, что Supabase запущен на VPS 147.45.143.147
2. Проверьте доступность порта 3000 (или вашего порта)
3. Убедитесь, что ANON_KEY правильный
4. Проверьте логи VS Code: View → Output → Cline
