# 🔄 Инструкция по перезапуску MCP сервера ITPI

## После изменения кода MCP сервера

Когда вы изменили файл `index.js` или другие файлы MCP сервера, необходимо перезапустить сервер.

## Способ 1: Перезапуск VS Code (РЕКОМЕНДУЕТСЯ)

1. Закройте VS Code полностью (Cmd+Q на Mac, Alt+F4 на Windows)
2. Откройте VS Code снова
3. MCP сервер автоматически перезапустится с новыми изменениями

## Способ 2: Перезапуск через Command Palette

1. Откройте Command Palette (Cmd+Shift+P на Mac, Ctrl+Shift+P на Windows)
2. Введите: `Developer: Reload Window`
3. Нажмите Enter

## Проверка работы новых инструментов

После перезапуска проверьте, что новые инструменты с префиксом `itpi_` доступны:

### Тестовые команды для Cline:

```
Используй itpi_list_tables чтобы показать все таблицы в базе ITPI
```

```
Используй itpi_execute_sql чтобы получить 5 записей из таблицы services
```

```
Используй itpi_execute_sql чтобы получить все задания со статусом published из таблицы tasks
```

## Доступные инструменты ITPI

После перезапуска у вас будут доступны:

- ✅ `itpi_list_tables` - список таблиц (ITPI проект)
- ✅ `itpi_execute_sql` - SQL запросы (ITPI проект)
- ✅ `itpi_list_users` - список пользователей (ITPI проект)
- ✅ `itpi_get_user` - информация о пользователе (ITPI проект)
- ✅ `itpi_execute_migration` - выполнение миграций (ITPI проект)

## Одновременная работа с двумя проектами

Теперь у вас есть **два независимых набора инструментов**:

### Для проекта SHEMA:
- `list_tables`
- `execute_sql`
- `list_users`
- `get_user`
- `execute_migration`

### Для проекта ITPI:
- `itpi_list_tables`
- `itpi_execute_sql`
- `itpi_list_users`
- `itpi_get_user`
- `itpi_execute_migration`

Оба набора работают **одновременно** без конфликтов!

## Устранение проблем

### Если инструменты не появились:

1. Убедитесь, что вы полностью перезапустили VS Code
2. Проверьте, что в `cline_mcp_settings.json` сервер `supabase-itpi-smeta` не отключен (disabled: false)
3. Откройте Output панель (View → Output) и выберите "Cline" для просмотра логов
4. Проверьте, что нет ошибок при запуске MCP сервера

### Если видите ошибки:

1. Проверьте файл `.env` - все ли переменные заполнены
2. Проверьте доступность Supabase API: `curl https://itpi.webtm.ru/rest/v1/`
3. Проверьте, что `SUPABASE_SERVICE_ROLE_KEY` правильный

## Логи MCP сервера

Чтобы увидеть логи MCP сервера:

1. View → Output
2. Выберите "Cline" из выпадающего списка
3. Найдите строки с "Supabase ITPI Smeta MCP сервер"

Успешный запуск выглядит так:
```
✅ Supabase ITPI Smeta MCP сервер запущен
```

## Дополнительная информация

- Изменения в коде MCP сервера требуют перезапуска VS Code
- Изменения в `.env` файле также требуют перезапуска
- Оба MCP сервера (shema и itpi) работают независимо
