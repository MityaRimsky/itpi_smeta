# ITPI Smeta - Система расчета смет для инженерных изысканий

Проект для автоматизации расчета стоимости инженерно-геодезических изысканий на основе СБЦ ИГДИ 2004.

## 🎯 Цель проекта

**Этап 1 (текущий):** Создание телеграм-бота для расчета стоимости работ
- Пользователь вводит название работ и объем
- Бот находит расценки в базе данных
- Бот рассчитывает обоснование, коэффициенты и общую стоимость

**Этап 2:** Разработка веб-приложения для полноценной работы со сметами

## 🗄️ Структура базы данных

Проект использует 11 таблиц для хранения нормативов и расчета смет:

### Основные таблицы нормативов
- **norm_docs** - Реестр нормативных документов (СБЦ ИГДИ 2004 и др.)
- **norm_items** - Расценки на работы (основные строки таблиц СБЦ)
- **norm_addons** - Добавочные начисления (фиксированные/процентные)
- **norm_coeffs** - Коэффициенты-множители (районные, сезонные и др.)

### Таблицы для расчета смет
- **estimates** - Шапка сметы (проект, заказчик, итоги)
- **estimate_lines** - Строки сметы (работы, объемы, расчеты)

### Справочные таблицы
- **inflation_indices** - Индексы инфляции для пересчета цен
- **regional_coeffs** - Региональные коэффициенты по субъектам РФ

### Шаблоны
- **projects** - Проекты (для группировки смет)
- **estimate_templates** - Шаблоны типовых смет
- **template_sections** - Разделы шаблонов

## 📁 Структура проекта

```
itpi_smeta/
├── mcp-servers/
│   └── supabase-itpi-smeta/     # MCP сервер для работы с БД
│       ├── index.js              # Основной код сервера
│       ├── package.json
│       ├── .env.example          # Пример конфигурации
│       └── README.md             # Документация MCP
├── migrations/
│   ├── 001_create_smeta_tables.sql   # Создание структуры БД
│   └── 002_seed_initial_data.sql     # Начальные данные из СБЦ
├── docs/
│   ├── СБЦ_ИГДИ_2004.pdf        # Справочник базовых цен
│   └── Примеры_смет.pdf          # Примеры готовых смет
├── DATABASE_SCHEMA.md            # Подробное описание структуры БД
├── MIGRATION_GUIDE.md            # Руководство по применению миграций
├── apply_migration.sh            # Скрипт для применения миграций
├── .env.supabase                 # Конфигурация подключения (не в git)
└── README.md                     # Этот файл
```

## 🚀 Быстрый старт

### 0. Запуск Telegram бота

```bash
# Перейдите в папку бота
cd bot

# Установите зависимости
pip install -r requirements.txt

# Настройте переменные окружения
cp .env.example .env
# Отредактируйте .env с вашими ключами

# Запустите бота
python main.py
```

Подробная документация по боту: [bot/README.md](bot/README.md)

### 1. Установка MCP сервера

```bash
cd mcp-servers/supabase-itpi-smeta
npm install
cp .env.example .env
# Отредактируйте .env с вашими данными Supabase
```

### 2. Применение миграций

```bash
# Через MCP сервер (рекомендуется)
# См. MIGRATION_GUIDE.md

# Или через psql
cp .env.supabase.example .env.supabase
# Отредактируйте .env.supabase
./apply_migration.sh migrations/001_create_smeta_tables.sql
./apply_migration.sh migrations/002_seed_initial_data.sql
```

### 3. Проверка установки

```sql
-- Проверка созданных таблиц
SELECT table_name FROM information_schema.tables 
WHERE table_schema = 'public' ORDER BY table_name;

-- Проверка загруженных данных
SELECT 
  (SELECT COUNT(*) FROM norm_docs) as docs,
  (SELECT COUNT(*) FROM norm_items) as items,
  (SELECT COUNT(*) FROM norm_coeffs) as coeffs,
  (SELECT COUNT(*) FROM norm_addons) as addons;
```

## 📚 Документация

- **[DATABASE_SCHEMA.md](DATABASE_SCHEMA.md)** - Подробное описание структуры БД, полей, индексов и триггеров
- **[MIGRATION_GUIDE.md](MIGRATION_GUIDE.md)** - Руководство по применению миграций и работе с данными
- **[MCP сервер](mcp-servers/supabase-itpi-smeta/README.md)** - Документация по MCP серверу

## 🔧 Возможности

### MCP сервер для работы с БД

Доступные инструменты:
- `itpi_list_tables` - Список таблиц в БД
- `itpi_execute_sql` - Выполнение SQL запросов с фильтрами
- `itpi_execute_migration` - Применение миграций
- `itpi_list_users` - Список пользователей
- `itpi_get_user` - Информация о конкретном пользователе

### Полнотекстовый поиск расценок

```sql
-- Поиск по названию работ
SELECT work_title, unit, price_field, price_office
FROM norm_items
WHERE search_text @@ plainto_tsquery('russian', 'топографический план масштаб 1:500')
ORDER BY popularity DESC, ts_rank(search_text, plainto_tsquery('russian', 'топографический план')) DESC
LIMIT 10;
```

### Расчет стоимости работ

```sql
-- Пример расчета стоимости съемки
WITH work AS (
  SELECT * FROM norm_items 
  WHERE work_title ILIKE '%топографический план%1:500%'
  LIMIT 1
),
coeffs AS (
  SELECT * FROM norm_coeffs 
  WHERE code IN ('SPECIAL_REGIME_1_25', 'SEASON_WINTER_1_3')
),
addons AS (
  SELECT * FROM norm_addons 
  WHERE code IN ('ORG_LIQ_6PCT', 'INTERNAL_TRANSPORT')
)
SELECT 
  work.work_title,
  work.price_field * 92 as base_field_cost,  -- 92 га
  work.price_office * 92 as base_office_cost,
  -- Применение коэффициентов и добавок
  -- ... (полный расчет в документации)
FROM work, coeffs, addons;
```

## 🔑 Переменные окружения

### .env.supabase (для psql миграций)
```bash
DB_HOST="localhost"
DB_PORT="55432"
DB_USER="supabase_admin.{TENANT_ID}"
DB_PASSWORD="your_password"
DB_NAME="postgres"
```

### mcp-servers/supabase-itpi-smeta/.env (для MCP)
```bash
SUPABASE_URL="https://itpi.webtm.ru"
SUPABASE_SERVICE_ROLE_KEY="your_service_role_key"
```

## 📖 Источники данных

Проект основан на официальных нормативных документах:

1. **СБЦ ИГДИ 2004** - Справочник базовых цен на инженерно-геодезические изыскания
   - Утвержден Госстроем России 23.12.2003 No 213
   - Цены приведены к базисному уровню на 01.01.2001
   - Включает расценки на все виды геодезических работ

2. **Примеры смет** - Реальные сметы из практики
   - Показывают применение коэффициентов
   - Демонстрируют структуру обоснований
   - Содержат расчеты прочих расходов

## 🎯 Следующие шаги

1. **Загрузить больше расценок** - добавить данные из других таблиц СБЦ
2. **Создать телеграм-бота** - для взаимодействия с пользователями
3. **Реализовать логику расчета** - автоматический подбор коэффициентов и добавок
4. **Добавить шаблоны смет** - для типовых видов работ
5. **Разработать веб-интерфейс** - для полноценной работы со сметами

## 🛠️ Требования

- Node.js >= 18
- PostgreSQL 14+ (через Supabase)
- psql (для прямых миграций)
- SSH доступ к серверу (опционально)

## 🤝 Вклад в проект

Проект находится в активной разработке. Приветствуются:
- Добавление новых расценок из СБЦ
- Улучшение алгоритмов поиска
- Оптимизация расчетов
- Документация и примеры

## 📄 Лицензия

MIT

---

**Статус проекта:** 🟢 Активная разработка (Этап 1)

**Последнее обновление:** Январь 2026
